# Biblioteca de cod `io`

Acesta este biblioteca input/output standard.

Pentru a folosi biblioteca `io`, va fi adusă în scope-ul programului prin declarația `use std::io;` în zona numită `prelude`.

Această bibliotecă de cod permite utilizatorului să introducă input.
